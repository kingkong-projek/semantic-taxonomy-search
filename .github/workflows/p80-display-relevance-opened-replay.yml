name: P80 display relevance opened replay

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/p80-display-relevance-opened-replay.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  replay:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Compile-check replay
        run: python -m py_compile scripts/replay_p80_display_relevance_opened.py
      - name: Replay frozen rule on opened stress
        run: python scripts/replay_p80_display_relevance_opened.py
      - name: Freeze diagnostic replay
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add research/evaluation/v31/p80-display-relevance-opened-replay-v0.json
          git commit -m 'research: freeze P80 display relevance opened replay'
          git push
