name: A593 breadth A B C once

on:
  push:
    paths:
      - .github/workflows/a593-breadth-abc-once.yml

permissions:
  contents: write
  actions: read

jobs:
  evaluate:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install diagnostic dependencies
        run: python -m pip install --disable-pip-version-check numpy scipy scikit-learn
      - name: Download frozen A593 teacher corpus
        uses: actions/download-artifact@v4
        with:
          name: gemma-a593-checkpoint
          path: artifacts/a593
          github-token: ${{ github.token }}
          run-id: 34232985249
      - name: Compile-check
        run: python -m py_compile scripts/evaluate_a593_breadth_proxy_abc.py
      - name: Evaluate frozen simple entrants
        run: >-
          python scripts/evaluate_a593_breadth_proxy_abc.py
          --teacher artifacts/a593/gemma-a593-teacher.jsonl
          --proxy research/evaluation/v31/compile-time-semantic-a593-breadth-proxy-v0.json
          --output artifacts/a593-breadth-proxy-abc.json
      - name: Freeze result and self-remove
        run: |
          cp artifacts/a593-breadth-proxy-abc.json research/evaluation/v31/compile-time-semantic-a593-breadth-proxy-abc.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          rm .github/workflows/a593-breadth-abc-once.yml
          git add research/evaluation/v31/compile-time-semantic-a593-breadth-proxy-abc.json .github/workflows/a593-breadth-abc-once.yml
          git commit -m 'research: freeze simple A B C breadth comparison'
          git fetch origin main
          git rebase origin/main
          git push
