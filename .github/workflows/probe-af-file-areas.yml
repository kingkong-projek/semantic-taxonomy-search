name: Probe AF file areas

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/probe_af_file_areas.py"
      - ".github/workflows/probe-af-file-areas.yml"

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Resolve concrete file areas and schemas
        run: python scripts/probe_af_file_areas.py
